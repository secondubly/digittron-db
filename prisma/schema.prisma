generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model command_permissions {
  name     String          @id @db.VarChar(255)
  level    PermissionLevel @default(VIEWER)
  commands commands?
}

model commands {
  name                String              @id @db.VarChar(255)
  aliases             Json?               @default("[]") @db.Json
  response            String
  enabled             Boolean             @default(true)
  visible             Boolean             @default(true)
  id                  Int                 @default(autoincrement())
  command_permissions command_permissions @relation(fields: [name], references: [name], onDelete: Cascade)
}

model settings {
  id            Int    @id @default(autoincrement())
  name          String @db.VarChar(255)
  oauth         String @db.VarChar(255)
  refresh_token String @db.VarChar(255)
  value         Json?  @default("null") @db.Json

  @@unique([id, name])
}

enum PermissionLevel {
  VIEWER
  FOLLOWER
  REGULAR
  VIP
  SUBSCRIBER
  MODERATOR
  EDITOR
  BROADCASTER
}
