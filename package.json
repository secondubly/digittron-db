{
  "name": "@secondubly/digittron-db",
  "version": "7.1.0",
  "description": "secondubly's server schema for digittron",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/secondubly/digittron-db.git"
  },
  "publishConfig": {
    "@secondubly/registry": "https://npm.pkg.github.com",
    "access": "public"
  },
  "scripts": {
    "generate": "npx prisma migrate",
    "postinstall": "tsc && cp src/index.d.ts dist/index.d.ts && npm run generate",
    "test": "exit 0"
  },
  "author": "secondubly",
  "license": "UNLICENSED",
  "devDependencies": {
    "@prisma/client": "^5.21.1",
    "dotenv-cli": "^7.4.2",
    "prisma": "^5.21.1",
    "prisma-types-export": "^1.0.0",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "@prisma/client": ">=5"
  }
}
